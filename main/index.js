const express = require("express/lib/config");
const nodemon = require("nodemon/lib/config");
const bodyParser = require("body-parser/lib/config");
const cors = require("cors/lib/config");
const app = express();

required;
